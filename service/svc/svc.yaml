apiVersion: v1
kind: Service
metadata:
  name: test-svc
  namespace: dev
  labels:
    app: test-svc
    service: test-svc
spec:
  selector:
    app: test-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
