apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: internal-svc-destination
  namespace: dev
spec:
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
    tls:
      mode: ISTIO_MUTUAL
  host: internal-svc
