apiVersion: v1
kind: Service
metadata:
  name: ineternal-test-svc
  namespace: dev
  labels:
    app: ineternal-test-svc
    service: ineternal-test-svc
spec:
  selector:
    app: ineternal-test-svc-red
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
