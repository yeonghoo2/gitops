apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: gradle-sample-destination
  namespace: dev
spec:
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
    tls:
      mode: ISTIO_MUTUAL
  host: gradle-sample
