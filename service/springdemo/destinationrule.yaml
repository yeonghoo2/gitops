apiVersion: networking.istio.io/v1alpha3
kind: Destination
metadata:
  name: demo-service-destination
  namespace: dev
spec:
  host: demo-service
  trafficPolicy:
    loadBalancer:
      simple: RANDOM
    tls:
      mode: ISTIO_MUTAL
  subsets:
  - name: blue
    labels:
      dev/deployment: blue
  - name: green
    labels:
      dev/deployment: green
