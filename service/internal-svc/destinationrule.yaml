apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: internal-test-svc-destination
  namespace: dev
spec:
  trafficPolicy:
    loadBalancer:
      simple: ROUND_ROBIN
    tls:
      mode: ISTIO_MUTUAL
  host: internal-test-svc
